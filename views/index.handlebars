<h1>Burger</h1>

<h2>Burgers Are Hot!</h2>

<ul>
  {{#each burger}}
    {{#unless devoured}}
<li>
	{{burger_name}}

	<button class="eat-burger" data-id="{{this.id}}">EAT DA BURGER!</button>
</li>

    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers Are Yummy!</h2>

<ul>
  {{#each burger}}
    {{#if devoured}}
<li>
	{{burger_name}}
	</li>
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bu">Burger Name:</label>
    <input type="text" id="bu" name="name">
  </div>

  <button type="submit">Add Burger</button>
</form>
